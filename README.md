# opencv build (docker)
 * build with Nonfree Module (SIFT, SURF)
 * build with opencl

### support platform
 * android (arm, mips, x86)
 * linux

### build example
`docker build -t sanghwa/opencv:3.2.0 .`
